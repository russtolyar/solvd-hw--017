<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
		PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
		"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.solved.mvchw017.persistence.PassportRepository">

	<insert id="create" keyColumn="staff.id" keyProperty="id" useGeneratedKeys="true">
		insert into Staff (kitchen_id,name, type) values (#{kitchenId},${staff.name},${staff.type})
	</insert>
</mapper>